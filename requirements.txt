# PDF text extraction + page rasterization for OCR fallback
PyMuPDF>=1.24

# OCR fallback (also requires the Tesseract binary installed on the system)
pytesseract>=0.3.10
Pillow>=10.0

# Token-aware chunking
tiktoken>=0.7

# OpenAI — embeddings + answer generation
openai>=1.0

# Relational metadata + vector store: PostgreSQL + pgvector
SQLAlchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
pgvector>=0.2

# Queue/cache + object-storage (S3/MinIO) clients
redis>=5.0
boto3>=1.34

# Config / secrets
python-dotenv>=1.0

# Backend API
fastapi>=0.110
uvicorn[standard]>=0.27
PyJWT>=2.8

# Dev
pytest>=8.0
